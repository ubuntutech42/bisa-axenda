# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure
#
# DATABASE_URL (Cloud SQL): configurar no Firebase Console > Backend > Settings > Environment
# ou via Secret Manager (env.variable + secret). Não commitar valor no repositório.

runConfig:
  maxInstances: 1
